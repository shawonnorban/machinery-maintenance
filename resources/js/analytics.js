/**
 * Dashboard, report and schedule routes only (Frontend 10.1).
 * Never loaded on the technician execution screen.
 */
import Chart from 'chart.js/auto';
import flatpickr from 'flatpickr';

window.Chart = Chart;
window.flatpickr = flatpickr;
